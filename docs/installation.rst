.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install parthsql

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv parthsql
    $ pip install parthsql
